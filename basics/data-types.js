// data types: number, string, boolean, undefined, objects, functions
// keyword for variable declaration: var, let, const

const name = 'John';
const isHoliday = false;
const person = {
    firstName: 'Allen',
    lastName: 'Green'
}
let num = 10;

num = 10 * 2;

console.log(name)
console.log(num)
console.log(`${person.firstName} ${person.lastName} ${num + 100}`)